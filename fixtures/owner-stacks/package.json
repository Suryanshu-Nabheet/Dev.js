{
  "name": "owner-stacks",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "devjs": "experimental",
    "devjs-dom": "experimental",
    "devjs-scripts": "5.0.1",
    "web-vitals": "^2.1.0"
  },
  "scripts": {
    "prestart": "cp -a ../../build/oss-experimental/. node_modules && rm -rf node_modules/.cache;",
    "start": "devjs-scripts start",
    "build": "devjs-scripts build",
    "test": "devjs-scripts test",
    "eject": "devjs-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "devjs-app",
      "devjs-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
