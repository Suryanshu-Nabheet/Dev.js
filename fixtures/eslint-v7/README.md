# ESLint v7 Fixture

This fixture allows us to test e2e functionality for `eslint-plugin-devjs-hooks` with eslint version 7.

Run the following to test.

```sh
cd fixtures/eslint-v7
yarn
pnpm build
pnpm lint
```
