# ESLint v10 Fixture

This fixture allows us to test e2e functionality for `eslint-plugin-devjs-hooks` with eslint version 10.

Run the following to test.

```sh
cd fixtures/eslint-v10
yarn
pnpm build
pnpm lint
```
