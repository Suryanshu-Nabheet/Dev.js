# ESLint v8 Fixture

This fixture allows us to test e2e functionality for `eslint-plugin-devjs-hooks` with eslint version 8.

Run the following to test.

```sh
cd fixtures/eslint-v8
yarn
pnpm build
pnpm lint
```
